# 2026-03-28 — Docs build + CI fixes

## Zakres
Naprawa błędów GitHub Actions dla dokumentacji (`website`) oraz domknięcie ostrzeżeń CI związanych z Node 20.

## Co zostało zrobione
- Zaktualizowano workflowy GitHub Actions do wersji zgodnych z migracją Node 24:
  - `actions/checkout@v5`
  - `actions/setup-node@v5`
  - `codecov/codecov-action@v5`
  - dodano `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true` (workflow-level/job-level tam, gdzie potrzebne).
- Naprawiono `src-electron/main.ts` (zgłoszenia lint z CI):
  - usunięto wskazane `any` na krytycznych liniach,
  - poprawiono obsługę `catch` na `unknown` + helpery,
  - usunięto nieużywane przypisania (`pluginBridge`, `autoUpdaterService`).
- Naprawiono post-job błąd submodułu:
  - usunięto ghost-submodule `services/mydia/repo` z indexu git (mode `160000`).
- Naprawiono build dokumentacji Docusaurus:
  - dodano brakujący plik `website/src/css/custom.css`,
  - poprawiono motyw Prism w `website/docusaurus.config.js` (`themes.github` / `themes.dracula`),
  - plugin Google Analytics ładowany warunkowo (tylko gdy ustawiony `GOOGLE_ANALYTICS_ID`),
  - uproszczono `website/sidebars.js` do `autogenerated`,
  - ustawiono `onBrokenLinks: 'warn'` (build nie blokuje się na legacy broken links).

## Wynik
- `Tests & Quality #115` — **completed/success**.
- `Deploy Documentation #6` — **completed/success**.
- Docs build lokalnie (`website`) przechodzi i generuje statyczne pliki (`build/`, `build/pl`).

## Commity
- `5e33993` — CI migration + lint fixes.
- `236e431` — docs build fix (Docusaurus config/sidebar/custom.css).
