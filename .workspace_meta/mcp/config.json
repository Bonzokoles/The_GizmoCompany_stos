{
  "mcpServers": {
    "_instructions": "Serwery MCP dla ZENO Browser. Klucze API ładowane z .env (gitignored). Używaj ${env:VARIABLE_NAME} dla secrets.",

    "zeno-browser-mcp": {
      "command": "node",
      "args": ["dist-electron/src-electron/mcp-server/mcp-server.js"],
      "env": {
        "ANTHROPIC_API_KEY": "${env:ANTHROPIC_API_KEY}",
        "NODE_ENV": "development"
      }
    },

    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${env:GITHUB_TOKEN}"
      }
    },

    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "./src", "./src-electron", "./config", "./docs"]
    },

    "fetch": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-fetch"]
    },

    "memory": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-memory"],
      "env": {
        "MEMORY_FILE": ".workspace_meta/mcp/memory.json"
      }
    }
  }
}
